<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Serve existing static asset files directly
  RewriteCond %{REQUEST_FILENAME} -f
  RewriteRule ^ - [L]

  # Rewrite all requests for subdomain/subpath to root index.html
  RewriteRule . /index.html [L]
</IfModule>
